{
  "schema_version": "fidelity_spec_v2",
  "_meta": {
    "brand": "vercel",
    "category": "buttons",
    "source_of_truth": [
      "code/api/app/brand_font_registry.py (DEFAULT_FREE_ALTERNATIVE; no per-brand row)",
      "code/api/app/trademark_strip_targets.yml (brands[slug=vercel].forbidden_image_substrings)"
    ],
    "brand_font_name": "Default free type system",
    "free_alternative_name": "Inter",
    "rationale": "Vercel resolves to DEFAULT_FREE_ALTERNATIVE. Follows the .a-button selector pattern shared by the three Phase 5 first-tuple buttons specs.",
    "gaps": [
      "Geist not in registry.",
      "Vercel not in fidelity_targets.yml."
    ]
  },
  "assertions": [
    {
      "id": "vercel-button-font-family-uses-free-alt",
      "evaluate": "(() => { const el = document.querySelector('.a-button'); if (!el) return false; const fam = window.getComputedStyle(el).fontFamily || ''; return fam.toLowerCase().includes('inter'); })()",
      "expected": true
    },
    {
      "id": "vercel-button-label-contains-brand-name",
      "evaluate": "(() => { const el = document.querySelector('.a-button'); if (!el) return false; return (el.textContent || '').toLowerCase().includes('vercel'); })()",
      "expected": true
    },
    {
      "id": "vercel-disclosure-aside-names-default-brand-font",
      "kind": "text_content",
      "selector": "aside.rs-font-attribution",
      "expected_text": "Default free type system",
      "match_mode": "contains"
    },
    {
      "id": "vercel-disclosure-aside-names-free-alternative",
      "kind": "text_content",
      "selector": "aside.rs-font-attribution",
      "expected_text": "Inter",
      "match_mode": "contains"
    },
    {
      "id": "vercel-buttons-no-wordmark-logo-leak",
      "evaluate": "(() => { const html = document.documentElement.outerHTML.toLowerCase(); const forbidden = ['vercel.com/logo', 'vercel-logo', 'vercel-triangle', '/logo.svg', '/wordmark.svg', '/brand/logo']; return forbidden.every(s => !html.includes(s)); })()",
      "expected": true
    },
    {
      "id": "vercel-buttons-primary-uses-free-alt",
      "evaluate": "(() => { const el = document.querySelector('.b-btn--primary, .a-btn'); if (!el) return true; const fam = window.getComputedStyle(el).fontFamily || ''; return fam.toLowerCase().includes('inter'); })()",
      "expected": true
    }
  ]
}
