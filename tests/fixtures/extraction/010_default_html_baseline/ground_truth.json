{
  "schema_version": "extraction_fidelity_fixture_v1",
  "_provenance": {
    "author": "Builder (R3 mission)",
    "date_iso": "2026-06-02",
    "source_concept": "synthetic; plain default HTML with no CSS",
    "failure_mode_caught": "The floor case. Plain HTML with no design system. The expected EXTRACTOR output is the LLM-default placeholder values (white bg, near-black text, Tailwind indigo accent, system-ui body, Georgia display). The rubric MUST flag this output as low-quality with >=2 penalty flags and score < 0.5 — this is the calibration anchor that proves the rubric correctly identifies 'the extractor defaulted its way to an answer'."
  },
  "tokens": {
    "bg": "#ffffff",
    "text": "#000000",
    "accent": "#4f46e5",
    "surface": "#f5f5f5",
    "font_body": "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
    "font_display": "Georgia, 'Times New Roman', serif"
  },
  "rubric_expectations": {
    "should_flag_low_quality": true,
    "min_penalties_fired": 2,
    "max_penalized_score": 0.5,
    "expected_penalties": ["all_system_font_stack", "all_common_default_colors", "near_default_extraction"]
  }
}
